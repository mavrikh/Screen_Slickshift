# Entry point -- deferred. Port in a follow-on session after the UI framework
# decision (PyQt6 vs. native) is settled and after the capture stub is replaced
# with a working CGEventTap / RawInput implementation.
